// 黑色HighChart主题
export const blackHighChartTheme = {
	chart: {
		backgroundColor: '#0F0F0F', // 背景为白色
		plotBackgroundColor: '#0F0F0F', // 绘图区背景白色
		plotBorderWidth: 0, // 去除边框
		plotShadow: false
	},
	xAxis: {
		lineColor: '#303030', // X轴线条为黑色
		tickColor: '#303030', // 刻度线为黑色
		labels: {
			style: {
				color: '#858587' // 标签文字为黑色
			}
		},
		title: {
			style: {
				color: '#858587' // X轴标题文字为黑色
			}
		}
	},
	yAxis: {
		gridLineColor: '#303030', // Y轴网格线为浅灰色
		labels: {
			style: {
				color: '#858587' // Y轴标签文字为黑色
			}
		},
		title: {
			style: {
				color: '#858587' // Y轴标题文字为黑色
			}
		}
	}
}

// 白色HighChart主题
export const whiteHighChartTheme = {
	chart: {
		backgroundColor: '#FFFFFF', // 背景为白色
		plotBackgroundColor: '#FFFFFF', // 绘图区背景白色
		plotBorderWidth: 0, // 去除边框
		plotShadow: false
	},
	colors: ['#1754F8']
	// xAxis: {
	// 	lineColor: '#000000', // X轴线条为黑色
	// 	tickColor: '#000000', // 刻度线为黑色
	// 	labels: {
	// 		style: {
	// 			color: '#000000' // 标签文字为黑色
	// 		}
	// 	},
	// 	title: {
	// 		style: {
	// 			color: '#000000' // X轴标题文字为黑色
	// 		}
	// 	}
	// },
	// yAxis: {
	// 	gridLineColor: '#E0E0E0', // Y轴网格线为浅灰色
	// 	labels: {
	// 		style: {
	// 			color: '#000000' // Y轴标签文字为黑色
	// 		}
	// 	},
	// 	title: {
	// 		style: {
	// 			color: '#000000' // Y轴标题文字为黑色
	// 		}
	// 	}
	// }
}
